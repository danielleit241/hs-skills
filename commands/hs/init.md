---
description: Create or refresh a concise, evidence-based project map for AI-assisted development.
argument-hint: "[project-path]"
---

Invoke `hs:init` with `$ARGUMENTS`. Create or update the project-root orientation documents only: `CLAUDE.md`, `AGENTS.md`, `system-architecture.md`, `code-standards.md`, and `project-overview-prd.md`.
